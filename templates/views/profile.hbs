{{!-- <!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Student Profile</title>
    <!-- Bootstrap CSS -->
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <!-- Custom CSS -->
    <style>
        body {
            background-color: #f0faff;
            /* Light bluish background color */
            font-family: Arial, sans-serif;
            color: #333;
        }

        .header {
            background: linear-gradient(135deg, #6A82FB, #AD94FF);
            padding: 50px 0;
            text-align: center;
            color: #fff;
            height: 150px;
        }

        .profile-picture {
            width: 150px;
            height: 150px;
            border-radius: 50%;
            overflow: hidden;
            margin: 0 auto;
            border: 5px solid #fff;
            box-shadow: 0px 0px 15px rgba(0, 0, 0, 0.2);
        }

        .profile-picture img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }

        .profile-info {
            background: linear-gradient(135deg, #5356FF 0%, #42A5F5 100%);
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0px 0px 15px rgba(0, 0, 0, 0.2);
        }

        .profile-info h2 {
            color: #5356FF;
            /* Blue heading color */
            margin-bottom: 20px;
        }

        .profile-info p {
            font-size: 16px;
            margin-bottom: 10px;
        }

        .profile-info .btn {
            background-color: #007bff;
            border: none;
        }

        .profile-info .btn:hover {
            background-color: #0056b3;
            /* Darker blue on hover */
        }
    </style>
</head>

<body>
    <div class="container-fluid header">
        <h1>Welcome to Your Profile</h1>
    </div>

    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="profile-info">
                    <div class="text-center">
                        <div class="profile-picture">
                            <img src="https://via.placeholder.com/150" alt="Profile Picture">
                        </div>
                    </div>
                    <h2 class="text-center">{{user.fname}} {{user.lname}}</h2>
                    <p class="text-center">Email: {{user.email}}</p>
                    <hr>
                    <div class="row">
                        <div class="col-md-6">
                            <p><strong>Student ID:</strong> {{user.registerID}}</p>
                            <p><strong>Batch:</strong> {{user.yog}}</p>
                            <p><strong>Major:</strong> Computer Science</p>

                        </div>
                        <div class="col-md-6">
                            <p><strong>Personal Information</strong> </p>
                            <p><strong>Phone:</strong> {{user.phone}}</p>
                            <p><strong>Blood Group:</strong> {{user.bloodgroup}}</p>
                            <p><strong>Date of Birth:</strong> {{user.dob}}</p>
                            <p><strong>Gender:</strong> {{user.gender}}</p>
                            <p><strong>Address:</strong> {{user.address}}</p>
                            <p><strong>State:</strong> {{user.state}}</p>

                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <br>
    <br>
    <br>


    <!-- Bootstrap JS and jQuery -->
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.5.2/dist/js/bootstrap.min.js"></script>
</body>

</html> --}}


<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Student Profile</title>
    <!-- Bootstrap CSS -->
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <!-- Custom CSS -->
    <style>
        body {
            background-color: #f0faff;
            font-family: Arial, sans-serif;
            color: #333;
        }

        .header {
            background: linear-gradient(135deg, #2548f5, #9374f7);
            padding: 50px 0;
            text-align: center;
            color: #fff;
            height: 150px;
        }

        .profile-picture {
            width: 150px;
            height: 150px;
            border-radius: 50%;
            overflow: hidden;
            margin: 0 auto;
            border: 5px solid #fff;
            box-shadow: 0px 0px 15px rgba(0, 0, 0, 0.2);
        }

        .profile-picture img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }

        .profile-info {
            background: linear-gradient(135deg, #d2efff, #e3dbfa);
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0px 0px 15px rgba(0, 0, 0, 0.2);
        }

        .profile-info h2 {
            color: #5356FF;
            margin-bottom: 20px;
        }

        .profile-info p {
            font-size: 16px;
            margin-bottom: 10px;
        }

        .profile-info .btn {
            background-color: #5356FF;
            border: none;
        }

        .profile-info .btn:hover {
            background-color: #8F94FB;
        }

        .profile-info .name{
            color: #3252f0;
            font-weight: 500;
        }

    </style>
</head>

<body>
    <div class="container-fluid header">
        <h1>Welcome to Your Profile</h1>
    </div>

    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="profile-info">
                    <div class="text-center">
                        <div class="profile-picture">
                            <img src="https://via.placeholder.com/150" alt="Profile Picture">
                        </div>
                    </div>
                    <br>
                    <h2 class="text-center name">{{user.fname}} {{user.lname}}</h2>
                    <p class="text-center"><b>Email:</b> {{user.email}}</p>
                    <hr>
                    <div class="row">
                        <div class="col-md-6">
                            <p><strong>Student ID:</strong> {{user.registerID}}</p>
                            <p><strong>Batch:</strong> {{user.yog}}</p>
                            <p><strong>Major:</strong> Computer Science</p>

                        </div>
                        <div class="col-md-6">
                            <p><strong>Personal Information</strong> </p>
                            <p><strong>Phone:</strong> {{user.phone}}</p>
                            <p><strong>Blood Group:</strong> {{user.bloodgroup}}</p>
                            <p><strong>Date of Birth:</strong> {{user.dob}}</p>
                            <p><strong>Gender:</strong> {{user.gender}}</p>
                            <p><strong>Address:</strong> {{user.address}}</p>
                            <p><strong>State:</strong> {{user.state}}</p>

                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <br>
    <br>


    <!-- Bootstrap JS and jQuery -->
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.5.2/dist/js/bootstrap.min.js"></script>
</body>

</html>
